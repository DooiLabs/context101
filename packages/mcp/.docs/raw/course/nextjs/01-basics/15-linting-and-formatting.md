# Linting and Formatting

Keep code style consistent.

Recommended tools:
- ESLint
- Prettier

Common script setup:

```json
{
  "scripts": {
    "lint": "next lint",
    "format": "prettier --write ."
  }
}
```
